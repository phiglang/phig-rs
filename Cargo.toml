[workspace]
members = [".", "conformance"]

[package]
name = "phig"
description = "Parser and serializer for phig"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
